name: first workflow (shell script)

on:
  push:
    branches:
      - main

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: List files
        run: ls -la
      - name: Run script
        run: ./script.sh